@model FluentValidationSample.Models.StudentViewModel
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title">Add Student</h4>
</div>

@using (Html.BeginForm())
{
    <div class="modal-body form-horizontal">
        <div class="form-group">
            @Html.LabelFor(model => model.Name, new { @class = "col-lg-4 col-sm-3 control-label" })
            <div class="col-lg-8 col-sm-9">
                @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Name)
            </div>
        </div>       
        <div class="form-group">
            @Html.LabelFor(model => model.Email, new { @class = "col-lg-4 col-sm-3 control-label" })
            <div class="col-lg-8 col-sm-9">
                @Html.TextBoxFor(model => model.Email, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Email)
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button data-dismiss="modal" id="btnCancel" class="btn btn-default" type="button">Cancel</button>
        <button class="btn btn-success" id="btnSubmit" type="submit">Submit</button>
    </div>
}
